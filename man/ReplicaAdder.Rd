% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/all_classes.R
\name{ReplicaAdder}
\alias{ReplicaAdder}
\title{Create a ReplicaAdder Module}
\usage{
ReplicaAdder(
  synth_pop,
  contingency,
  target_attribute,
  group_by = character(),
  missing_group_strategy = "borrow",
  warning_threshold = 0.05
)
}
\arguments{
\item{synth_pop}{A synthetic population represented as a
data.frame or data.table.}

\item{contingency}{A contingency table describing the
expected relationship between conditioning variables and
the target attribute.}

\item{target_attribute}{Character string identifying the
attribute to be assigned.}

\item{group_by}{Character vector specifying conditioning
variables used during assignment.}

\item{missing_group_strategy}{Character string specifying
how groups missing from the contingency table should be
handled. Options include \code{"borrow"}, \code{"overall"} and
\code{"error"}.}

\item{warning_threshold}{Numeric significance threshold
used when generating validation warnings.}
}
\value{
A \code{ReplicaAdder} object.
}
\description{
Creates a \code{ReplicaAdder} object for assigning attributes
to a synthetic population using contingency tables.
}
\details{
The resulting module can subsequently be configured using
\code{renew()}, executed using \code{enhance()} and validated using
\code{ratify()}.
}
\examples{
age_gender <- data.frame(
  age_group = c("18-64", "65+"),
  count = c(10, 10)
)

population <- make_agents(
  age_gender
)

contingency <- data.frame(
  age_group = c("18-64", "65+"),
  education = c("Degree", "School"),
  count = c(60, 40)
)

adder <- ReplicaAdder(
  synth_pop = population,
  contingency = contingency,
  target_attribute = "education",
  group_by = "age_group"
)

}
\seealso{
\code{\link{ReplicaAdder}},
\code{\link{renew}},
\code{\link{enhance}},
\code{\link{ratify}}
}
